export interface SitePostContent {
    slug: string;
    partSlug: string;
    title: string;
    image: string;
    preview: string;
    publishMonth: string;
    publishDate: string;
    body: string;
}
